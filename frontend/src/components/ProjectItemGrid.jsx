import React from 'react'

const ProjectItemGrid = (item) => {
  const data = {
    thumbnail: item.thumbnail,
    title: item.title,
    customer: item.customer
  }
  return (
    <div>ProjectItemGrid</div>
  )
}

export default ProjectItemGrid